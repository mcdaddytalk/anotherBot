const { initCurrency } = require('./currency');
const { initAlarms } = require('./alarms');
const { initMiners } = require('./miners');

module.exports = {
    initCurrency,
    initAlarms,
    initMiners,
};
